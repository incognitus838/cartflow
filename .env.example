# PostgreSQL — CartFlow uses the "cartflow" schema (safe alongside bank-app on same Neon DB)
# Local: direct Neon host. Vercel: use -pooler host (sync-vercel-env.mjs does this automatically).
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.region.aws.neon.tech/neondb?sslmode=require"

# Session / auth secret (32+ random characters — never commit real values)
AUTH_SECRET="replace-with-a-long-random-secret"

# App URL (production Vercel URL or http://localhost:3001 for dev)
NEXT_PUBLIC_APP_URL="http://localhost:3001"

# Cache bust after reseed/scripts (any random string)
REVALIDATE_SECRET="replace-with-a-random-secret"

# Vercel CLI token for npm run vercel:sync-env (https://vercel.com/account/tokens)
# VERCEL_TOKEN=""

# Paystack (https://paystack.com)
PAYSTACK_SECRET_KEY=""
PAYSTACK_PUBLIC_KEY=""

# Flutterwave (https://flutterwave.com)
FLUTTERWAVE_SECRET_KEY=""
FLUTTERWAVE_PUBLIC_KEY=""

# Email via ZeptoMail (https://www.zoho.com/zeptomail/) — transactional only
# 1. Create an Agent, verify cartflow.com.ng (SPF/DKIM DNS in ZeptoMail dashboard)
# 2. Agent → SMTP/API → copy Send Mail Token
ZEPTOMAIL_SEND_TOKEN=""
# Optional: force provider (auto-detects ZeptoMail when token is set)
# EMAIL_PROVIDER="zeptomail"
#
# Verified sender addresses in your ZeptoMail Agent:
NOTIFICATION_FROM_EMAIL="orders@cartflow.com.ng"
TRANSACTIONAL_FROM_EMAIL="hello@cartflow.com.ng"
#
# Legacy Resend (optional fallback if ZEPTOMAIL_SEND_TOKEN is unset):
# RESEND_API_KEY=""

# SMS notifications (https://twilio.com) — optional
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_FROM_NUMBER=""